# Lumen — post-redesign status log

One-line-per-loop ledger for the v7 post-redesign roadmap. Each loop
appends; nothing else writes here. The UI redesign's own ledger lives
in `docs/redesign/STATUS.md` (closed at loop 20).

Canonical plan: `~/.claude/projects/-home-ubuntu-projects-E-Learning-Platform/planning/post-redesign-2026-05-26/plan-v7.md`.

Roadmap summary (v7-locked): L19.5 → L20.5 → L20.6 → L21-Sec → L21a →
L21b → L22 → L23 → L24 → L25 → L26 → L27 → **L28 (interview-ready)** →
L29 → L30 → L31 → L32-L37 (cuttable polish) → L38-L40 (audit + rename
+ distribution).

| Loop | Surface | Outcome | Commit |
|------|---------|---------|--------|
| 19.5 | Founding story (README opener) + empty `/blog` index | Shipped. README leads with the locked V6-F6 paragraph; `/blog` renders `EmptyState` until L30 case-study posts arrive. 51 files / 286 tests green (+1 file / +2 tests). i18n keys added in `en` + `ar` (728 keys → 733 each, parity test green). Sitemap updated. | `c5b36a8` |
| 20.5 | TS Generics/Variance course + `/demo` deep-link + runtime-flags endpoint + ADRs 17/18/19 | Shipped. 4 modules / 8 lessons + canonical-error lesson seeded; `/demo` redirects to `/learn/typescript-variance?tutor=open&q=…&lesson=canonical-error`; `feature_tutor_streaming` exposed via `GET /api/v1/runtime-flags` (default OFF until L21b); three ADRs draft Celery prefork pool + Redis Streams + atomic phase fence/after-commit enqueue. 52 files / 288 frontend tests green; 4 new backend tests green. | `5c27373` |
| 20.6 | RAG-from-scratch course (8 lessons, self-referential) + 15-question curated demo library + GET /api/v1/demo-questions + streaming observability tile placeholder | Shipped. Library locks canonical_id=ts-variance-canonical with invariant guards; expected_tools validated against known sub-agents; 5 categories (retriever-only, retriever-code-runner, retriever-web-searcher, refusal, multi-hop). Streaming tab on /admin/observability shows 6 placeholder tiles + ADR cross-refs. 53 files / 289 frontend tests; +10 backend tests (RAG seed + demo-questions library). | `a9e41a5` |
| 21-Sec | Security hardening (no streaming yet) — Llama 3.x special-token sanitizer + Sentry tutor-namespace scrubber + Lua cost-cap + concurrency scripts (microcents, zero FP drift) + code-runner subprocess RLIMIT hardening + email-verify grandfather migration + boot-hook backstop + empty tutor_turn_jobs table with reservation columns + seed-in-prod refusal + IDOR contract tests | Shipped. +43 backend tests, all green. Backend suite 697/697. Alembic 0027 grandfathered users + created tutor_turn_jobs table. All primitives additive; L21a wires the callers. | `41e66f8` |
| 21-Sec rescue | Codex pass — 2 P1 findings: (1) Alembic `:data::jsonb` bind is invalid (SQLAlchemy reads `::` as PG cast operator); fix → `CAST(:data AS jsonb)`; (2) boot-hook grandfather had no cutoff, would silently auto-verify any user who registered post-deploy. Fix → `Settings.l21sec_deploy_timestamp` + `AND created_at < :cutoff` in the UPDATE. Both regression-tested. | `2ab45c1` |
| 21a | Streaming spine (flag OFF) — 4 new endpoints (POST /tutor/turns, status, SSE stream, cancel) all gated 503 until L21b; Celery `tutor.run_turn.v1` task with atomic phase fence + `after_commit` enqueue + finally-suppress cleanup; sweep beat (10s pending / 30s running) + orphan-stream cleanup beat (5min); Redis Streams helpers with trim detection; AsyncIterator orchestrator (noop event sequence). +7 backend tests. Suite 701 → 708 green. | `ac3211d` |
| 21b | Frontend streaming + flag-flip wire — SSE parser (multi-line/CRLF/mid-chunk edge cases), fetch-based SSE client (custom-header / Bearer-token capable), iOS Safari 15.0-15.3 UA sniff, `useTutorStream(turnId)` hook via `useSyncExternalStore`, new `StreamingTutorPanel` with tools-list + accumulating text + cursor animation + `aria-live="polite"`, `TutorPanel` branches on `flags.tutor_streaming && supportsStreaming()` (legacy impl renamed inline). +14 new frontend tests (parser 9 + UA 5). Suite 53/289 → 55/303. **Code lands flag-OFF; prod env `FEATURE_TUTOR_STREAMING=true` is the actual flip.** | `e975504` |
| 22 | Demo-question chip rail above the composer (consumes useDemoQuestions library; canonical-first; lime-tinted canonical chip; aria-labeled), "Tools used" label reframe (was "Agent thinking"; en + ar), RTL leak fix at studio/draft-trace-timeline.tsx:94 (`text-left` → `text-start`). +2 new frontend tests. Suite 55/303 → 56/305. Backend untouched. | `969fbfc` |
| 22 rescue | Codex pass on L21a→L22 — 3 findings: (1) SSE handler used `$` for initial subscriptions → would miss early events (fix: `0-0` initial, `Last-Event-ID` on resume); (2) `_data_dict.__str__()` is Python repr, not JSON → reducer can't parse (fix: `json.dumps`); (3) `mark_terminal` overwrote any prior terminal status → cancellation race (fix: `WHERE status NOT IN ('complete','failed','aborted')` + returns bool). Regression tests in `test_tutor_terminal_race.py`. Backend 708 → 711 green. | `1e2407a` |
| 23 | Cost-cap closing CTA (`<CostCapClosingCta />`) replaces generic 503/429 surface on cost-cap errors (`llm.budget_exceeded`, `tutor.{user,ip,global}_cap`). Wired into both legacy + streaming tutor panels via shared `isCostCapError(err)` helper. Optional reset-timer + Calendly link. i18n in en + ar. **Approaching-cap banner deferred** until L21a cost-reserve callers land. +7 frontend tests. Suite 56/305 → 57/312. Backend untouched. | TBD |
| 24 | Mobile/tablet pass — tutor in bottom Sheet (90vh) below `lg`; inline column at `lg+`. Review grade buttons `h-11` for WCAG 2.2 AA 44px touch-target. Closes AUDIT §7 row 16. **Canonical mobile screenshot deferred** until `FEATURE_TUTOR_STREAMING=true` flips in prod (the screenshot needs the streaming UI). 57 files / 312 tests green; backend untouched. | TBD |
| 25 | Eval instrumentation — adversarial probe corpus (15 probes across 5 categories) at `apps/backend/evals/security/probes.jsonl` + string-match scorer (`refused` / `leaked` / `ambiguous`) at `app/evals/adversarial.py` + headline `refusal_rate` metric. Baseline-comparison wire shape at `app/evals/baseline.py` (BaselineScore + BaselinePair + compute_deltas + aggregate_pairs). +11 backend tests. Real comparison runs + streaming-eval harness deferred until LLM-budget + AsyncOpenAI follow-up. | TBD |
| 26 | `/admin/evals` UI — `<Sparkline />` SVG component (64×16, no chart lib) + 8-run trend per SuiteCard. text-primary (lime) tint; oldest left, focus dot on rightmost. ariaLabel reads "Trend over last N runs (oldest left)". +5 frontend tests. Suite 57/312 → 58/317. Backend untouched. | `751965a` |
| 27 + 28 | Public `/eval` surface + `/eval/methodology` (interview-ready milestone). Honest-empty contract holds until first sealed run is promoted. | `283f625` |
| 29 | Animated agent-replay hero on landing — CSS keyframe SSE replay, reduced-motion override, tool-row opacity fade-in removed (axe contrast). | `1b009b4` |
| 30 | `/case-study` long-form narrative + sitemap entry + `<Button asChild>` sweep on nested `<Link>`s. | `8d42fb1` |
| 31 | OG cards (8 routes) + README portfolio block + `docs/release/screencap-script.md`. | `d86b085` |
| 26→31 rescue | Codex pass — 2 findings: invalid nested `<Link><Button>` interactive content; `/case-study` missing from sitemap. + e2e dual-mount fix (mobile Sheet reverted) + axe contrast on hero tool-rows. | `fb472ce` + `ee9faf1` + `cc24d37` |
| 31-followup | Operator handoff doc + 5-step runbook + AsyncOpenAI streaming wired in `llm_stream.py` (real tokens via `LLM_PROVIDER=openai/groq-compat`; Anthropic branch raises until L37). +4 llm_stream tests + autouse `noop` fixture for orchestrator tests. | `94326da` + `fdc7747` |
| ops/flip-flag | `flip-flag.yml` workflow (manual `workflow_dispatch`, FEATURE_*/LUMEN_* whitelist, atomic `.env.production` edit, `up -d --no-deps` recreate (was `restart` — env wasn't re-read), 120s smoke budget). Flag flipped on prod 2026-05-27. | `4f412c3` |
| 32 | pgvector retrieval wiring in `orchestrate_stream` + course-context columns on `tutor_turn_jobs` + `course_slug` in `NewTurnIn` (resolves to course_id, 404 on miss) + Celery task runs `find_relevant_chunks` before invoking orchestrator + `[L:lesson_id]` citation contract folded into synth system prompt. +7 backend tests (5 orchestrator + 2 endpoint). Alembic 0028 adds `course_id` (FK SET NULL) + `user_message` columns. | `e050589` |
| 33 | Cost-cap + concurrency reservation at POST /tutor/turns (check_concurrency → reserve_cost against 3 buckets → 429 with tagged error code; releases slot on cost-cap rejection). Celery task reconciles delta on turn_complete. 5 new Settings knobs + 4 new AppError classes. **Real bug found:** `docker-compose.prod.yml` x-api-env anchor never forwarded FEATURE_TUTOR_STREAMING (or any TUTOR_*) — explains why L32 deploy left the flag off in prod despite .env.production having the value. **Prod `/runtime-flags` now returns `tutor_streaming: true`.** +5 backend tests. Suite 22 streaming-tutor tests total. | `a1a4953` |
| 34 | Legacy POST `/tutor/conversations/{id}/messages` applies the same L21-Sec defences as streaming (check_concurrency + reserve_cost → 429 with `tutor.{user,ip,global}_cap` or `tutor.too_many_concurrent`). Both paths now write `tutor_turn_jobs` rows for unified observability (legacy is synchronous, no Celery). Lean cut vs full orchestrator unification — the 1218-LoC multi-agent dispatcher stays. +3 backend tests. | `cfa5d4e` |
| 32→34 rescue | Codex pass — 5 P1 findings. (1) Cost-bucket key prefix mismatch: L33 wrote `tutor_cost:*` but sweep reconciles `cost:*` — every reservation orphaned on worker crash. (2) Reservation leak when POST 404s on unknown course_slug (resolve now runs BEFORE reserve). (3) Cancel-pending leak: DELETE handler now reads + releases the reservation directly. (4) Legacy reconcile actual=0 bypassed cap entirely → now reconciles with `actual=reserved` (zero net delta, bucket retains spend). (5) Legacy post-reserve failure leak → unified outer try/except. Also: L32 course_slug now restricted to `CourseStatus.published`. | `005ad23` |
| 35-38 bundle | (35) Mobile bottom-Sheet via SSR-safe `useMediaQuery` (`useSyncExternalStore`) — single panel mounts, fixes L24 dual-mount that broke Playwright. (36) `run_one_item` + `run_comparison` runner closures in `baseline.py` — operator path can now drive real providers + score closures. (37) Anthropic streaming wired in `llm_stream.py` via `client.messages.stream()` + `text_stream`; same StreamChunk contract as OpenAI. (38) Frontend `@sentry/nextjs` 8.x + 3 runtime configs + shared `beforeSendScrub` mirroring backend L21-Sec scrubber; `withSentryConfig` wraps `next.config.ts`; DSN-less builds are no-ops. +11 backend tests + 9 frontend tests. Verified `pnpm build` succeeds without DSN. | `5de881b` |
| 39 | Anti-abuse rate limit `@limiter.limit("20/minute")` on POST /tutor/turns. SSE resume hardening: `runWithRecovery` retries once with Last-Event-ID on stream error, polls /status on `trim_detected` until terminal. **Codex rescue on L35-L38 folded in** — 3 P1 Sentry scrubber gaps (extra/contexts, fetch breadcrumb data payloads, request URL query strings) + 1 P2 baseline runner per-item try/except w/ on_item_error callback. +1 backend test (rate limit) + 5 frontend tests (scrubber) + 1 backend test (preserve-on-fail). | `fee18d2` |
| 40 | **Closes the post-redesign arc.** Codex rescue on L39 — 2 P1 + 3 P2: (1) Sentry scrubMap made recursive (was 1-level → contexts.tutor.request.prompt leaked); (2) breadcrumb data dict walks scrubMap first (was zeroing only payload/body/url → nested `request.prompt` survived); (3) pollUntilTerminal per-request 3s AbortController via composeAbort polyfill (was: stuck fetch hangs the 60s budget); (4) SSE clean-EOF post-loop marks `tutor.stream_eof` (was: silent exit, snapshot stuck in synth); (5) on_item_error callback raises now propagate (was: contextlib.suppress defeated circuit-breaker contract). +3 tests (2 frontend scrubber + 1 backend baseline-abort). Final verification — backend `ruff check` + `ruff format` clean; 52 targeted tests pass; full frontend suite 342/342 pass. | `a562705` |
| 41 | **Operator-driven bundle.** (a) `MistralProvider` (OpenAI-compat inheritance) + `_stream_chat_openai_compat` shared core, free-tier friendly. (b) `GET /api/v1/eval/public` + `python -m app.cli promote-eval` ledger — narrow summary fields, honest-empty until promoted. (c) `python -m app.evals.run_baseline` typer CLI that wraps L36 `run_comparison` against real providers + LLM-as-judge. (d) `.github/workflows/prod-seed.yml` — approval-gated workflow that fixes the demo `/learn/typescript-variance` 404 by running `LUMEN_ALLOW_PROD_SEED=1 demo-seed`. Mistral env vars added to docker-compose.prod.yml `x-api-env` anchor. **Mistral key live-probed against api.mistral.ai → 200 OK.** +9 backend tests (4 mistral + 5 eval_public); 35 targeted tests pass. | TBD |
